//Exercício 1:

const prompt = require("prompt-sync")();

let numero = Number(prompt("Digite um número: "));
let resultado = numero ** 2;

console.log("O resultado da potência é:", resultado);



/* Descrição do exercício sobre potência:

Este exercício tem como objetivo calcular o quadrado de um número digitado pelo usuário.

Primeiramente, o programa solicita um número utilizando o prompt. Em seguida,
esse valor é armazenado na variável "numero".

Depois, é realizada a operação de potência utilizando o operador **,
e o resultado é armazenado na variável "resultado".

Por fim, o valor calculado é exibido no console.
*/


// Exercício 2:

/*
const prompt = require("prompt-sync")();

let base = Number(prompt("Digite a base: "));
let expoente = Number(prompt("Digite o expoente: "));

let resultado = base ** expoente;

console.log("O resultado da potência é:", resultado);


/* Descrição do exercício sobre potência:

Este exercício tem como objetivo calcular a potência de um número,
utilizando base e expoente digitados pelo usuário.

Primeiramente, o programa solicita a base e o expoente.
Os valores são armazenados nas variáveis "base" e "expoente".

Em seguida, é realizada a operação de potência utilizando o operador **,
armazenando o resultado na variável "resultado".

Por fim, o valor calculado é exibido no console.
*/