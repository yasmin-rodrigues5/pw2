// Exemplo 1
let comprimento = "hello";
let mundo = "world";
console.log(comprimento + " " + mundo);

// Exemplo 2
let nome="diva ";
let sobrenome=" divonica";

let nomeCompleto= nome+sobrenome;
console.log(nomeCompleto);

/*  Primeiro foi criado as variaveis que logo receberam seu valor, logo, no console.log foi feito a 
concatenacao de ambas utilizando o +.

Já no exemplo 2, também foi criada e declarada os valores das variaveis chamadas nome e sobrenome,
 porém, nesta, tambem foi criada a variavel sobreNome que como declarada recebeu a junção das ambas
 variaveis anteriores, e logo printada no console*/ 